import { PlaceholderPage } from '../../components/PlaceholderPage'

export function LoginPage() {
  return (
    <PlaceholderPage
      title="Đăng nhập"
      hint="/login - nhập email và mã OTP (làm sau)"
    />
  )
}
